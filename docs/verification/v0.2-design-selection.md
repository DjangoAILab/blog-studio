# v0.2 Design Selection Record

**Status:** selected and approved for implementation. **Prepared:** 2026-08-04.
**Approved:** 2026-08-05.

## Candidate gate

PASS: exactly three materially different candidates exist and each represents
first run, unified content library, editor/Markdown preview, and ChangeSet
review on desktop and mobile. See the durable candidate specification and
artifact index in
[`v0.2-interaction-target.md`](../design/v0.2-interaction-target.md).

The built-in image-generation retry failed at the image-edit backend with a
network error. The owner had explicitly authorized an interactive HTML fallback,
so no fourth visual direction was created. The fallback was visually inspected,
interaction-tested, and compared with the existing Studio at the same state.

## Selection

- Selected candidate: **Focused Workbench, top-navigation direction 3, refined**
- Selected by: **repository owner**
- Selected at: **2026-08-05 (Asia/Shanghai)**
- Requested adjustments before implementation: replace the rigid outer frame
  and inconsistent alignment with a continuous canvas, optical column anchors,
  one rounded grouped content surface, and a consistent radius/type scale. Use
  established accessible component and motion libraries instead of a bespoke
  imitation of Apple controls.

The approved desktop reference is:

```text
/Users/wang/.codex/generated_images/
  019fcaea-eb62-7121-8f00-c4245a2a1e61/
  exec-a765512a-668b-4609-9052-8f1ddd15407f.png
```

Implementation dependencies are `@base-ui/react`, `motion`, semantic plain-CSS
tokens, and a progressive `backdrop-filter` fallback. Third-party refraction
effects are not a baseline dependency because content and controls must remain
usable when blur, transparency, or advanced SVG filtering is unavailable.

The production visual gate is open. Each implemented surface still requires
desktop/mobile visual evidence, keyboard/focus checks, reduced-motion and
reduced-transparency verification, and relevant browser journeys before its
checklist item can pass.
