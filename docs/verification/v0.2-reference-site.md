# v0.2 Real Reference Site Verification

Verified read-only on 2026-08-04 against
`/Users/wang/Project/github/wangerzi/blog` at commit
`da94f63a35e39e7061de2e92b0821a5e8dbda777`.

## Contract exercised

The opt-in integration test creates only a temporary Blog Studio configuration
and SQLite database. It loads the real repository through `WorkspaceService`,
then uses the same `SiteService.discover()` and `SiteService.register()` public
contract as a new installation. Publishing is configured as `none`; no build,
preview subprocess, Provider, network request, canonical write, or production
database is involved.

Discovery verified:

- display identity `wj2015-blog` and canonical URL
  `https://blog.wj2015.com/`;
- Hexo detection, native drafts, enhanced-preview capability, and publishing
  disabled;
- 93 published posts and 5 native drafts;
- filesystem resources with PDF/ZIP/text support while preserving `static` as
  a protected legacy prefix;
- Git branch `master`, clean worktree, and immutable head; and
- a registered public Site record that does not expose the technical workspace
  ID or filesystem paths.

The real source contains 1,590 files below `source/static` (295 MiB in the
current checkout). The `_config.yml` SHA-256 remained
`d9d1564125028c6eeffb22b767d98e5dc1a3fa5fcf1512af72a3855bc3bee600`.

## Reproducible evidence

```sh
CI=true \
BLOG_STUDIO_REFERENCE_ROOT=/Users/wang/Project/github/wangerzi/blog \
corepack pnpm --filter @blog-studio/studio exec vitest run \
  server/test/reference-site.test.ts
```

Result: 1 file and 1 test passed in 390 ms.

The test records the repository head, porcelain status, and source Hexo
configuration hash before loading. After discovery, temporary-database
registration, and rediscovery, it requires all three values to match exactly.
The observed worktree remained clean at the commit above.

## Deliberately not claimed yet

- This proves local compatibility and Site registration, not a home-server
  migration or UI onboarding journey.
- Enhanced real-theme preview was already proved for the same 93-post repository
  in v0.1; the v0.2 browser surface still waits for the selected visual direction.
- No production release was initiated.
