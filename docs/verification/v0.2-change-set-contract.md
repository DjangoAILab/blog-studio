# v0.2 ChangeSet and Local Commit Verification

Verified on 2026-08-05 on branch `codex/v0.2-site-foundation`.

## Contract now covered

- `Prepare changes` reads the Site's working copies and local Git state into an
  immutable SQLite ChangeSet. It does not write canonical content, run the
  generator, contact a publish/cache Provider, push Git, or verify a public URL.
- Source revisions, full working-copy bodies/front matter and versions,
  referenced resource records and content hashes, the Site configuration hash,
  Git head/branch, managed diffs, untracked paths, ignored paths, staging state,
  and current file hashes participate in the frozen review or fingerprint.
- Identical inputs return the existing prepared ChangeSet. A changed working
  copy or repository state creates a new ChangeSet and supersedes the old review.
- Apply rechecks the Git head/state, Site configuration, every source and draft
  revision, and every referenced resource. A stale input invalidates the review
  with `CHANGE_SET_CONFLICT` and is not overwritten.
- Git working-tree entries are normalized by path before fingerprint comparison,
  so nondeterministic ordering between tracked and untracked entries cannot
  manufacture a conflict. Genuine head, configuration and working-tree changes
  report the exact changed boundary.
- Apply is journaled before canonical writes. Synchronous failure rolls back
  already-written documents; startup recovery rolls back a recognizable target
  after an interrupted apply and refuses ambiguous external content.
- Local commit is a separate operation after apply. The API requires an editable
  message and explicit paths, rejects paths outside the review, requires every
  applied document, and records the resulting commit ID.
- The local Git adapter commits only selected paths, preserves unrelated staged
  work, restores the exact pre-operation index if commit creation fails, and
  never pushes.
- The Site-scoped remote-release endpoint accepts only a committed ChangeSet and
  an explicit confirmation phrase, records both immutable identifiers, and
  builds from a detached Git worktree at that commit rather than the live tree.
- The review sheet exposes document before/after content, resource metadata,
  every repository state and Provider diffs. Branch, base revision, prepared
  time, fingerprint, exact commit paths and the resulting commit ID stay visible
  at the relevant decision boundary.
- Apply requires a separate checkbox confirmation. Document paths are mandatory
  for local commit while unrelated repository paths start unchecked. Remote
  release appears only after commit, uses a visually separate risk boundary,
  requires the exact `RELEASE COMMITTED CHANGESET` phrase, and shows stage,
  cancellation, failure and rollback controls.
- If the repository or configuration changes after preparation, the review
  stays recoverable: it explains the optimistic conflict and offers `按最新状态
重新准备`, after which the owner reviews and confirms the new frozen record.
- Release list, detail, cancellation, and rollback now have Site-scoped routes;
  the production ChangeSet journey does not need to reveal a workspace ID.

## Reproducible evidence

```sh
corepack pnpm --filter @blog-studio/persistence test
```

Result: 7 files and 27 tests passed, including migration v3, immutable
preparation, supersession, apply journals, and atomic apply-state completion.

```sh
corepack pnpm --filter @blog-studio/adapter-local-git test
```

Result: 1 file and 3 tests passed, covering read-only status/diff inspection,
selected-path commit with unrelated staged work, and exact index restoration on
commit failure.

```sh
corepack pnpm --filter @blog-studio/studio exec vitest run \
  server/test/app.test.ts -t "prepares immutable idempotent ChangeSets"
```

Result: the Site API journey passed prepare, repeated prepare, supersession,
review history, apply, explicit-path local commit, unrelated-file preservation,
stale-apply rejection, and a separately confirmed release. The release fixture
proved that an uncommitted live-tree edit was absent from the published output.

```sh
corepack pnpm --filter @blog-studio/studio exec vitest run \
  server/test/change-sets.test.ts
```

Result: the simulated crash/restart test restored the original source from the
durable apply journal and left the ChangeSet review recoverable. The same suite
also changes the Site configuration and then a referenced resource after
preparation; both attempts invalidate the frozen review before canonical writes.
Its successful-apply case reverses identical Git status entries between prepare
and apply, proving order normalization does not weaken content comparison.

```sh
corepack pnpm --filter @blog-studio/studio e2e
```

Result: the two-test browser suite passed in 13.1 seconds. Its disposable
authoring journey created and discarded a
native draft, then edited and reloaded an existing published article before
preparing it. The fixture then adds an unrelated repository file after
preparation, proves the first apply is rejected, uses the visible re-prepare
action, reviews the refreshed repository state, separately confirms apply,
created a selected-path local commit, typed the remote-release confirmation and
observed release progress. The fixture used a temporary Git repository, fake
Hexo executable, filesystem publish directory and `example.invalid`; it was
deleted at process exit and could not mutate a production blog or Provider.

```sh
CI=true corepack pnpm check
CI=true corepack pnpm format:check
```

Result: all 17 package lint, type, test, production-build, policy,
documentation-link, formatting, and patch-whitespace gates passed. Studio
passed 11 files and 71 active tests, with one intentionally skipped reference
fixture.

## Compatibility boundary retained

- The legacy workspace-scoped release endpoint returns HTTP 410 by default.
  A server-only compatibility option can temporarily re-enable it for controlled
  v0.1 migration tests. The Site UI never invokes that route.
