# v0.2 Home-server Release Candidate

Verified on 2026-08-06 for the real `wj2015-blog` installation. This is an
authoring/control-plane upgrade acceptance record, not a content-release claim.
No canonical Markdown, Git revision, generated public site, or remote Provider
was changed.

## Exact candidate

- Candidate input manifest (241 files):
  `sha256:17ab55225d8a077aded67ddb204b163003b09b3e706bb42577604fcbf3ce380a`.
- Retained hermes source:
  `/tmp/blog-studio-v02-rc17-exact.ewwcmz/source`.
- Hermes image: `blog-studio:v02-rc-17ab5522`.
- Home-server image: `blog-studio:home-v02-17ab5522`.
- Exact image ID on both hosts:
  `sha256:bae5d3287e451cb07db911b1ef61892a169799bd56ff28deb286581fe97e18a1`.
- OCI revision:
  `worktree-17ab55225d8a077aded67ddb204b163003b09b3e706bb42577604fcbf3ce380a`.
- Runtime user: `node`; bundled Git: 2.52.0.

Quick-start, hardened-container, and operations smokes passed against this
exact image on isolated hermes ports. The hardened smoke fetched an exact
Markdown-preview resource capability without an ambient session Cookie. The
source and saved image both passed the pinned Trivy 0.69.3 critical,
secret, and misconfiguration scans.

## Preserved production boundary

- Service directory: `/home/wang/services/blog-studio`.
- Canonical workspace revision before and after every operation:
  `da94f63a35e39e7061de2e92b0821a5e8dbda777`.
- Workspace status before and after every operation: clean.
- Public inventory aggregate before/after switch, rollback, forward recovery,
  and final cold recreation:
  `f22ddabf8f1c0c6ce4bc97c51d24d3fcb64f5e5b8a90c873f115205e8aa72080`.
  The aggregate is `sha256sum` over the sorted, public-root-relative file list.
- `/static/reading.jpeg` before/after:
  `22bd07ecf69d2e63d8634b6f9e31e069763ec3b700140bea9982a2a242e49fbd`.
- No file in `public/` was created or modified during the acceptance journey.

Only `studio` was recreated. The public blog, workspace, cache, storage, and
release Provider were not switched or invoked.

## Fresh recovery point

The production online-backup script created:

`runtime/backups/blog-studio-backup-20260806T060925Z.tar.gz`

- SHA-256:
  `ecc161ec7f909b04cef0be18fba4656e922bb534e617a54584b3f5ccd3f0e587`.
- Size: `619103106` bytes.
- Archive and checksum modes: `0600`; owner: `wang:wang`.
- `sha256sum -c`: PASS before the switch and after final cold recreation.
- Inventory includes metadata, the online SQLite snapshot, production config,
  and the complete non-generated workspace including Git metadata.

Earlier independently verified backups remain available as additional recovery
points.

## Authentication, Site, and real content

The owner initialized the password through the trusted CLI. Final status is
generation 1; browser login succeeds and survives cold container recreation.
The production setup endpoint reports:

```json
{
  "ready": true,
  "credentials": { "state": "ready" },
  "configuration": { "state": "valid" },
  "site": { "state": "registered" }
}
```

The registered `wj2015-blog` Site reports Hexo capabilities and 98 documents:
93 published plus 5 native drafts. The browser opened the real published article
“给 NAS 内网服务自动配置免费 HTTPS”, created a temporary SQLite working copy,
autosaved it, reloaded the application, and reopened the persisted working copy.
The temporary marker was then discarded; the final modified count and SQLite
working-copy count are both zero.

## Real preview and preparation journey

- Markdown mode rendered the real working copy in a `sandbox=""` iframe.
- All 21 local images used the five-minute exact-reference capability route.
  The first image completed with `naturalWidth=2482`; its request needed no
  ambient login Cookie.
- Site-theme mode completed in 11.27 seconds and did not claim false readiness:
  the generated route was absent, so the UI said “主题没有生成当前文章页，已自动回到
  Markdown 预览。” and kept the Markdown result usable.
- “准备更改” created a durable frozen review for one article. The review said
  “冻结记录未写入文件”; it was closed without applying, committing, invoking a
  Provider, or releasing.
- After the final cold recreation, the authenticated browser again loaded the
  Site/content library with zero modified documents and loaded the same 21
  Markdown images through newly issued capability URLs.

## Performance evidence

The reproducible local production-build fixture passed all interaction budgets:
list p95 7.9 ms, autosave p95 2.5 ms, navigation-frame p95 9.0 ms, and zero
frames over 50 ms. The real authenticated HTTPS path was additionally sampled
as an operational baseline: list p95 187.0 ms across 50 requests and autosave
p95 230.5 ms across 30 acknowledged writes. Its temporary working copy was
discarded. Network/TLS latency is deliberately outside the loopback product
budget.

## Switch, rollback, and cold recovery

The prior environment is retained as
`.env.pre-v02-17ab5522-20260806T061408Z`. The service first switched from
`blog-studio:home-v02-13f4ad92` to the exact final image, then rehearsed an
application rollback to the prior image and forward recovery. Health, setup,
workspace cleanliness, public inventory, and representative asset hashes passed
at every step.

The final post-journey cold recreation stopped and removed the Studio container,
then created a new one from the exact image. The healthy final container is
`9c777a6dd0c6ffab92d4cbaf117d1063248b90e354604d7357a0764d0efb7722`.
Credential generation, Site registration, authenticated content/preview,
workspace revision, public hashes, and backup checksum all survived.

## Safety result

No production apply, Git commit, Provider call, cache purge, object-storage
write, or content release occurred. Any future production content release still
requires a new, explicit, contemporaneous owner confirmation.

The owner was informed that the current credential appeared in an historical
acceptance-task tool log and explicitly accepted continued use without rotation
on 2026-08-06. The credential file remains outside the repository with mode
`0600`. This is a recorded owner risk acceptance, not proof of credential
rotation or remediation; future rotation remains recommended.
