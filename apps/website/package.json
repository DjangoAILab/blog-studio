{
  "name": "@blog-studio/website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "audit:lighthouse": "corepack pnpm build && node scripts/audit-lighthouse.mjs",
    "build": "corepack pnpm docs:generate && astro build && node scripts/check-links.mjs",
    "dev": "corepack pnpm docs:generate && astro dev",
    "docs:generate": "node scripts/generate-reference.mjs",
    "e2e": "corepack pnpm build && playwright test",
    "lint": "corepack pnpm docs:generate && astro sync && eslint src/index.ts src/content.config.ts test e2e playwright.config.ts vitest.config.ts",
    "preview": "astro preview",
    "test": "vitest run",
    "typecheck": "corepack pnpm docs:generate && astro check"
  },
  "dependencies": {
    "@astrojs/starlight": "0.41.6",
    "@fontsource-variable/ibm-plex-sans": "5.3.0",
    "@fontsource-variable/newsreader": "5.3.0",
    "astro": "7.1.6"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.62.1",
    "chrome-launcher": "1.2.1",
    "lighthouse": "13.4.1"
  }
}
