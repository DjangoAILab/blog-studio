# Reference blog compatibility evidence

Date: 2026-08-02

Reference commit: `65ac267f6fa44cb367694f906f59a4f6d85f0c9b`

Scope: read-only production baseline plus an isolated local clone. No reference
source file, COS object, DNS record, or CDN setting was changed.

## Production baseline

- `https://blog.wj2015.com/` returned HTTP 200 through Tencent CDN backed by
  Tencent COS.
- DNS used the existing `blog.wj2015.com.cdn.dnsv1.com.cn` CNAME.
- The source repository was clean and contained 93 posts and 475 files below
  `source/static/assets` (about 177 MiB).
- `_config.yml` checksum:
  `d9d1564125028c6eeffb22b767d98e5dc1a3fa5fcf1512af72a3855bc3bee600`.
- Existing publisher checksum:
  `69767b8dff31615039249c38c5b955ce704052006ae155f39f4100996f46605b`.
- The permalink remains `:year/:month/:day/:title/`; legacy resources remain
  under `/static/**`.
- Twelve deterministic legacy resource samples returned HTTP 200 and matched
  the generated source bytes by SHA-256. Twelve deterministic article,
  archive, and tag URLs returned HTTP 200.

The legacy publishing command remains the immediate rollback path until the
controlled production promotion is separately approved by all staging gates.

## Cold clone and build

An isolated, no-hardlink clone was installed from `yarn.lock` with Node
22.21.1 and Yarn 1.22.22. The original repository was not mounted writable.

| Check | Result |
| --- | ---: |
| Cold dependency install | 1 s |
| Hexo production build | 4 s |
| Generated files | 1,961 |
| Generated HTML files | 351 |
| Generated `static/**` files | 1,590 |
| Generated bytes | 324,644,864 |
| Output inventory SHA-256 | `f0a414082949b464332f522b3bfb1f768be58887a2e1d1ac3f2aa9141ae21d34` |
| Clone worktree after build | clean |

Every one of the 93 documents was parsed, read, and submitted as an unchanged
write through `HexoGeneratorAdapter`; all operations reported no change and all
source-file hashes remained byte-identical.

## Real Studio API journey

The built Studio server loaded the cloned reference workspace with a local-only
filesystem target. Authentication, CSRF, scan, discovery, read, real-theme
preview, preview proxy, and cleanup were exercised through HTTP.

| Operation | Observation |
| --- | ---: |
| Login | 23 ms |
| Workspace list | 3 ms |
| Compatibility scan | 5 ms, detected |
| List 93 posts | 33 ms |
| Read selected post | 23 ms |
| Cold real Hexo preview | 5,401 ms, 1,969 files |
| Fetch rendered article | 3 ms, HTTP 200 |

The preview response used the sandbox CSP and resolved the selected article to
its established URL path.

## Compatibility defect found and closed

One historical document uses `2022-07-014 23:25:51`. Hexo 6 normalizes that
legacy value to 2022-07-14, while the initial adapter rejected it. The adapter
now reproduces Hexo's three-digit-day normalization while rejecting impossible
calendar dates. The exact historical article now previews at its existing
`/2022/07/14/.../` path, and unit coverage preserves the behavior.

## Remaining staging gates

- Read the account's exact COS bucket, region, target prefix, CDN product, and
  cache rules from the authenticated Tencent console. The macOS session was
  locked during this audit, so this remains pending rather than inferred.
- Publish to an isolated COS prefix/origin with mounted secret files.
- Run upload, network, cache, restart, retry, and rollback fault injections.
- Import or otherwise establish a verified baseline before any bucket-root
  promotion. A first release must never treat existing objects as unowned
  additions.
- Compare complete production URL/output inventories and then perform one
  controlled change with the legacy command retained for rollback.
