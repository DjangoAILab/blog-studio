# v0.1.0 release verification — 2026-08-04

## Outcome

Blog Studio v0.1.0 is a public, non-draft, non-prerelease GitHub release. Its
annotated SSH-signed tag, source archive, documentation assets, checksums, and
multi-platform GHCR image were independently re-read or downloaded from their
published locations after the release workflow completed.

- release: [Blog Studio v0.1.0](https://github.com/DjangoAILab/blog-studio/releases/tag/v0.1.0);
- workflow: [Release run 30874639382](https://github.com/DjangoAILab/blog-studio/actions/runs/30874639382), passed in 8 minutes 28 seconds;
- source commit: `b155292701e6dbb6c14db8b5c199054c8a10ed35`;
- annotated tag object: `28cb8546195c9d4ebe4ed0bcd3772274f72d593a`;
- GitHub tag verification: `verified: true`, reason `valid`, verified at
  `2026-08-04T03:24:06Z`;
- signer: `Jeffrey Wang <admin@wj2015.com>`, SSH Ed25519 fingerprint
  `SHA256:8zhHvprjieNu4VOnr9AercGO8iFoYCdIiP39maaHlUs`.

The release workflow checked the annotated signature before it received package
write permission. It then built the image, generated and self-verified the
artifacts, and published the release. Every workflow step completed successfully.

## Published assets

The six published assets were downloaded into a new temporary directory with
`gh release download`. Running
`bash scripts/verify-release-artifacts.sh <download-directory>` against those
downloads passed the exact-file-set, checksum, metadata, archive-prefix, and
required-document checks.

| Asset                       |   Bytes | SHA-256                                                            |
| --------------------------- | ------: | ------------------------------------------------------------------ |
| `blog-studio-v0.1.0.tar.gz` | 368,541 | `c10683d0d5c0f2adc2651f4bc8e4e940b4beb2a6bd3c03e59833fb5828c82edb` |
| `container-digest.txt`      |     104 | `e6e783d3c947bbe17f72985fbf3684d5364bb1916569b9da2d0ba8026a11330a` |
| `release-metadata.json`     |     515 | `bce2089dda6f0c8557ee3fb5d51f2b2e5c4dad3fdca728d9eb3b170890d94dd8` |
| `RELEASE_NOTES.md`          |   1,965 | `6edb61163430a68a4cdcf6f9f72fb6fdcdde525267f880189c574ce411bfe347` |
| `SHA256SUMS`                |     427 | `ce30dfad2878f1bc2010f6391b5ab0380e26ea967ef77e019731f304ab455964` |
| `UPGRADE.md`                |   1,961 | `d57cfbcaee6f8a6cabb49b4b6654c7244e6c06637065def938da8dfb2599348a` |

`release-metadata.json` named the same source commit and this immutable image:

```text
ghcr.io/djangoailab/blog-studio@sha256:1748e8f94cef9154d09b95d43d9090b2610a7f1eb7eaa93ae8e3f1b331e4a5bd
```

## Published container

`docker buildx imagetools inspect` resolved the immutable reference to an OCI
index with Linux `amd64` and `arm64` image manifests. Both platforms also have
attestation manifests produced by the release build's provenance and SBOM
settings.

The immutable image was then pulled from GHCR, rather than reused from the
release runner. Its OCI labels report version `0.1.0`, license `Apache-2.0`, and
revision `b155292701e6dbb6c14db8b5c199054c8a10ed35`. Running
`scripts/container-smoke.sh` with that exact digest passed:

- non-root UID/GID and a read-only root filesystem;
- healthy startup and an unauthenticated API boundary;
- authenticated document discovery and durable autosave;
- clean SIGTERM handling and draft recovery after a cold restart.

## Production continuity after release

The release operation did not mutate the existing blog. A fresh post-release
probe compared all sixteen production sample paths against the phase B
postflight baseline by status, byte length, and SHA-256; all matched exactly.
`blog-editor.internal.wj2015.com/api/health` returned `200`, unauthenticated
workspace access returned `401`, and the home-server container remained running
and healthy as UID/GID `1000:1000` with a read-only root filesystem.

The running home-server revision remains
`8d2332913085aadc32963a517657636fad8704f1`. The diff from that revision to the
v0.1.0 source commit contains only README, documentation, checklists, and
verification evidence. No runtime source, dependency, image definition, or
deployment configuration differs, so no availability-neutral Studio restart
was introduced solely to change its OCI revision label.

The superseded adoption credential remains disabled and the scoped writer
credential remains active. Their local secret files are retained in the
administrator-controlled home-server directory with owner `wang:wang`, mode
`0600`, and read-only container mounts. Credential replacement is event-driven;
routine scope and last-use reviews do not require reissuing a working key.
